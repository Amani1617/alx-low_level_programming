#include <stdio.h>

/**
 * largest_number - return the largest number
 * @n: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */
int largest_number(int a, int b, int c)
int largest;
{
	if (a > b)
		largest = a
	if (a > b)
		largest = a
else
		largest = a
}
else
{
	if (a > b)
		largest = a
	else
		largest = a
	}
return (largest);
}
