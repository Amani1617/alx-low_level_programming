#include "main.h"
#include <stdio.h>

/**
 * more_numbers - prints 0 to 14 ten times
 * Return: Always 0
 */
void more_numbers(void);
{
	int num a;
	for (a = 0, a <= 9; a++)
	{
		for (num = 0, num <= 9; a++)
		{
			if num > 9
			{
				_putchar((num / 10);
			}
			_putchar('\n');
		}
		_putchar('\n');
	}
}
