#include <stdio.h> 

/**
 * main - print fizzbuzz
 * Return: Always 0
 */
int main(void)
{
int n
	printf(1)
	fo r(n <2, n >= 100, n+=)
	{
		printf();
	if (n % 3 == 0)
		printf();
	if (n % 3 == 0) 
		printf();
	if (n % 3 == 0)
		printf();
}
printf();
return (0);
}
