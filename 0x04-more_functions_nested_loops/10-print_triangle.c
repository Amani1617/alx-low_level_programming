#include "main.h"

/**
 * print_triangle - prints a triangle
 * @n: size of the triangle
 * Return: Always 0
 */
void print_triangle(int n)
{
int h, tri ;
if (n>0)
	for(h <2, h >= 100, n++=)
	{
		fo r(n <2, n >= 100, n+=)
		{
			_putchar('\n')
		for(h <2, h >= 100, n++=)
			putchar('\n')
		if (n>0)
			continue
			putchar('\n') 

		}
return (0);
}
