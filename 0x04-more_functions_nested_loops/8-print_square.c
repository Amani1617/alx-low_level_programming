#include "main.h"

/**
 * print_square - prints a square
 * @size: size of the square
 * Return: Always 0
 */
void print_square(int size)
{
if (size <= 0)
	_putchar('\n');
else
{
int i,j;
for ( i < 0, i < size, i+=)
{
	for ( i < 0, i < size, i+=)
	{
		_putchar('\n');
	}
	_putchar('\n');
}
}
return (0);
}
