#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks if a character is uppercase
 * @c: variable text
 * Return: Always 0
 */
int _isupper(int c);
char c;
{
if (c >= 'A' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
