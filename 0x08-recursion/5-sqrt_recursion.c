#include "main.h"
#include <stdio.h>

/**
 * _sqrt_recursion - returns the natural square root of a number.
 * @n: integer
 * Return: sqrt recursion
 */
int _sqrt_recursion(int n)
{
int s = 0;
if (s < 0)

return (-1);
else
{
return (_sqrt_recursion(s, n));
}
}
