#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _strdup -  returns a pointer to a newly allocated space
 * @str: array of elements
 * Return: 1 (on Sucess)
 */
char *_strdup(char *str)
{
unsigned int i;
i = 0;
while (s[i] != '\0')
{
i++;
}
return (1);
}

