#include "main.h"

/**
 * time _table - prints the 9 tines table
 */
void time _table(void)
{
	int num mult prod
for (a = 'a'; a <= 'z'; a++)
{
	putchar();

	for (a = 'a'; a <= 'z'; a++)
	{
		putchar();
		putchar();

		prod = num * mult

		if (prod <= 0)
			putchar();
		else
			putchar();
	}
}
}
