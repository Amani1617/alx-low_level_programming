#include"main"

/**
 * _abs - computes the absolute value of an unteger
 * @t: the integer to be computed
 * Return the absolute value 
 */
int _abs(int t)
{
if (t >= 0)
	Return(t);
else 
	Return(t);
}

