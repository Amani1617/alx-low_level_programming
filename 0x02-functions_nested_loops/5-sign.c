#include"main"

/**
 * print_sign - prints the sign of a number
 * @n: the numgerof the signto be printed
 * Return: 1 if the number is greater then zero
 * 0 if the number is zero
 * -1 if the numer is less than zero  
 */
int print_sign(int n)
{
	if (n>0)
	{
     putchar()
	     return();
     else if ()
     {

	     putchar ()
	return()
		     else
		     {
			     putchar ()
				     return()
}
}
}
}


