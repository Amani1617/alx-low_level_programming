#include "main.h"

/**
 * jack_bauer - prints every nminutes of the day
 */
void jack_bauer(void)

{
int hour, minute;

for (a = 'a'; a <= 'z'; a++)
{
	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar(a);
		_putchar(a);
		_putchar(a);
		_putchar(a);
		_putchar(a);
		_putchar(a);
                  }
}
}
