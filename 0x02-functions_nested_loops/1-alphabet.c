#include "main.h"
#include <stdio.h>

/**
 * print_alphabet - prints the alphabet in lowercase
 * Return: always 0
 */
void print_alphabet(void)
{
print_alphabet();

char la;

for (la = 'a'; la <= 'z'; la++)
	putchar(la);
putchar('\n');

return (0);
}
