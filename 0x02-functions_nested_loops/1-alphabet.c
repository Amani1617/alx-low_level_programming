#include "main.h"
#include <stdio.h>

/**
 * print_alphabet - prints the alphabet in lowercase
 * Return: always 0
 */
int main(void)
{

char la;

for (la = 'a'; la <= 'z'; la++)
	putchar(la);
putchar('\n');

}
